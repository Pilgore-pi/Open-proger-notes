
на клиенте:

```bsl
// Номер текущей строки, начиная с нуля
текСтрока = элементы.Таблица.ТекущаяСтрока;

// Структура, содержащая все поля строки
текСтрока = элементы.Таблица.ТекущиеДанные;
```

#1С #1С/Формы