
Тип `System.Collection.ObjectModel.ObservableCollection<T>`

По функциональным возможностям класс похож на класс `List<T>`. Основное отличие состоит в том, что данный класс может извещать внешние объекты об изменениях коллекции

| Конструктор                                          | Описание                     |
| ---------------------------------------------------- | ---------------------------- |
| `ObservableCollection<T>()`                          | Создание пустой коллекции    |
| `ObservableCollection<T>(IEnumerable<T> collection)` | Создание на основе коллекции |

| Метод                              | Описание                                                                                                         |
| :--------------------------------- | :--------------------------------------------------------------------------------------------------------------- |
| `Move(int oldindex, int newindex)` | Перемещает элемент со старым индексом на новый индекс, элементы левее нового индекса смещаются к старому индексу |
| `Add()`                            | Добавляет новый элемент                                                                                          |
| `CopyTo()`                         |                                                                                                                  |
| `Contains()`                       | Проверяет, содержит ли коллекция данный элемент                                                                  |
| `Clear()`                          | Очищает коллекцию                                                                                                |
| `IndexOf()`                        |                                                                                                                  |
| `Insert()`                         |                                                                                                                  |
| `Remove()`                         |                                                                                                                  |
| `RemoveAt()`                       |                                                                                                                  |


#C-Sharp #C-Sharp/Collections #Data_structures