1) `typeof(Type)` — возвращает объект Type, который имеет указанный тип.
2) `nameof(new List<int>().Count)` — возвращает имя сущности кода.
3) `sizeof(Type)` — получает количество байт, занимаемых указанным типом. В общем случае использование этого оператора требует unsafe-контекст, но если в качестве параметра `Type` будет один из базовых значимых типов или `enum`, то unsafe-контекст необязателен, так как `sizeof()` вернет предопределенную константу. Тип `Type` должен представлять `unmanaged` тип.

----

GENERATED WITH Claude Sonnet 4.5

## 1. Оператор `typeof()`

### Назначение

Возвращает объект типа `Type`, содержащий метаданные о типе.

### Синтаксис

```csharp
Type type = typeof(ИмяТипа);
```

### Основные характеристики

- Вычисляется **во время компиляции**
- Работает только с **именами типов** (не с переменными)
- Возвращает объект `System.Type`

### Примеры использования

```csharp
// Получение типа встроенных типов
Type intType = typeof(int);
Type stringType = typeof(string);

// Получение типа пользовательских классов
Type personType = typeof(Person);

// Работа с обобщенными типами
Type listType = typeof(List<>);           // Открытый обобщенный тип
Type listIntType = typeof(List<int>);     // Закрытый обобщенный тип

// Получение информации о типе
Console.WriteLine(intType.Name);          // Int32
Console.WriteLine(intType.FullName);      // System.Int32
Console.WriteLine(intType.Namespace);     // System
Console.WriteLine(intType.IsValueType);   // True
```

### Практическое применение

```csharp
// 1. Проверка типов
public void ProcessData(object data)
{
    Type dataType = data.GetType();
    
    if (dataType == typeof(string))
    {
        Console.WriteLine("Это строка");
    }
    else if (dataType == typeof(int))
    {
        Console.WriteLine("Это целое число");
    }
}

// 2. Создание экземпляров через рефлексию
Type type = typeof(Person);
object instance = Activator.CreateInstance(type);

// 3. Получение методов класса
Type myType = typeof(MyClass);
MethodInfo[] methods = myType.GetMethods();

foreach (var method in methods)
{
    Console.WriteLine(method.Name);
}

// 4. Работа с атрибутами
Type type = typeof(MyClass);
var attributes = type.GetCustomAttributes(typeof(ObsoleteAttribute), false);
```

## 2. Оператор `nameof()`

### Назначение
Возвращает **строковое имя** переменной, типа, метода или члена класса.

### Синтаксис

```csharp
string name = nameof(элемент);
```

### Основные характеристики
- Вычисляется **во время компиляции**
- Возвращает **простое имя** (без пространства имен)
- Проверяется компилятором (защита от опечаток)
- Появился в **C# 6.0**

### Примеры использования

```csharp
// Переменные
int count = 10;
Console.WriteLine(nameof(count));  // "count"

// Типы
Console.WriteLine(nameof(String));  // "String"
Console.WriteLine(nameof(List<int>));  // "List"

// Свойства и методы
public class Person
{
    public string Name { get; set; }
    
    public void SayHello()
    {
        Console.WriteLine(nameof(SayHello));  // "SayHello"
        Console.WriteLine(nameof(Name));      // "Name"
    }
}

// Параметры
public void SetValue(int value)
{
    Console.WriteLine(nameof(value));  // "value"
}
```

### Практическое применение

```csharp
// 1. Проверка аргументов
public void SetName(string name)
{
    if (name == null)
        throw new ArgumentNullException(nameof(name));
        // Вместо: throw new ArgumentNullException("name");
}

// 2. Реализация INotifyPropertyChanged
public class Person : INotifyPropertyChanged
{
    private string _name;
    
    public string Name
    {
        get => _name;
        set
        {
            _name = value;
            OnPropertyChanged(nameof(Name));  // Безопасно!
            // Вместо: OnPropertyChanged("Name");
        }
    }
    
    public event PropertyChangedEventHandler PropertyChanged;
    
    protected void OnPropertyChanged(string propertyName)
    {
        PropertyChanged?.Invoke(this, 
            new PropertyChangedEventArgs(propertyName));
    }
}

// 3. Логирование
public void ProcessData(string data)
{
    Logger.Log($"Вызван метод {nameof(ProcessData)}");
}

// 4. Маршрутизация в ASP.NET
[HttpGet]
public IActionResult Index()
{
    return RedirectToAction(nameof(Details));
    // Вместо: return RedirectToAction("Details");
}

// 5. Работа со словарями
var dict = new Dictionary<string, int>
{
    [nameof(Person.Age)] = 30,
    [nameof(Person.Name)] = 0
};
```

### Преимущества `nameof()`

✅ Рефакторинг безопасен (переименование через IDE)  
✅ Проверка во время компиляции  
✅ IntelliSense поддержка
✅ Избежание магических строк

## 3. Оператор `sizeof()`

### Назначение
Возвращает **размер типа в байтах** в неуправляемой памяти.

### Синтаксис
```csharp
int size = sizeof(тип);
```

### Основные характеристики
- Вычисляется **во время компиляции** для простых типов
- Работает только с **неуправляемыми типами**
- Возвращает `int` (размер в байтах)
- Для пользовательских типов требуется **unsafe контекст**

### Размеры встроенных типов

```csharp
Console.WriteLine(sizeof(byte));    // 1
Console.WriteLine(sizeof(sbyte));   // 1
Console.WriteLine(sizeof(short));   // 2
Console.WriteLine(sizeof(ushort));  // 2
Console.WriteLine(sizeof(int));     // 4
Console.WriteLine(sizeof(uint));    // 4
Console.WriteLine(sizeof(long));    // 8
Console.WriteLine(sizeof(ulong));   // 8
Console.WriteLine(sizeof(float));   // 4
Console.WriteLine(sizeof(double));  // 8
Console.WriteLine(sizeof(decimal)); // 16
Console.WriteLine(sizeof(char));    // 2
Console.WriteLine(sizeof(bool));    // 1
```

### Работа с пользовательскими типами

```csharp
// Структура должна содержать только неуправляемые типы
public struct Point
{
    public int X;
    public int Y;
}

// Требуется unsafe контекст
unsafe
{
    Console.WriteLine(sizeof(Point));  // 8 (4 + 4)
}

// Или в настройках проекта включить unsafe код
// И пометить метод как unsafe
public unsafe void GetSize()
{
    int size = sizeof(Point);
}
```

### Примеры с выравниванием

```csharp
unsafe
{
    // Структура с выравниванием
    struct MyStruct1
    {
        public byte a;   // 1 байт
        public int b;    // 4 байта
    }
    // Размер: 8 байт (не 5!), из-за выравнивания
    
    // Упакованная структура
    [StructLayout(LayoutKind.Sequential, Pack = 1)]
    struct MyStruct2
    {
        public byte a;   // 1 байт
        public int b;    // 4 байта
    }
    // Размер: 5 байт
    
    Console.WriteLine(sizeof(MyStruct1));  // 8
    Console.WriteLine(sizeof(MyStruct2));  // 5
}
```

### Практическое применение

```csharp
// 1. Выделение буферов
unsafe
{
    int bufferSize = sizeof(int) * 100;
    byte* buffer = stackalloc byte[bufferSize];
}

// 2. Работа с неуправляемой памятью
unsafe void CopyData()
{
    int dataSize = sizeof(MyStruct);
    IntPtr ptr = Marshal.AllocHGlobal(dataSize);
    // ... работа с памятью
    Marshal.FreeHGlobal(ptr);
}

// 3. Сериализация
public byte[] Serialize<T>(T data) where T : unmanaged
{
    unsafe
    {
        int size = sizeof(T);
        byte[] buffer = new byte[size];
        fixed (byte* ptr = buffer)
        {
            *(T*)ptr = data;
        }
        return buffer;
    }
}

// 4. Оптимизация памяти
struct OptimizedStruct
{
    // Знание размеров помогает оптимизировать порядок полей
    public byte a;    // 1
    public byte b;    // 1
    public short c;   // 2
    public int d;     // 4
    // Итого: 8 байт (оптимально упаковано)
}
```

### Ограничения

```csharp
// ❌ НЕ работает с управляемыми типами
// sizeof(string);     // Ошибка компиляции
// sizeof(object);     // Ошибка компиляции
// sizeof(List<int>);  // Ошибка компиляции

// ✅ Альтернатива для управляемых типов
string str = "Hello";
int size = System.Text.Encoding.UTF8.GetByteCount(str);
```

## Сравнительная таблица

| Оператор | Возвращает | Время вычисления | Требует unsafe | C# версия |
|----------|-----------|------------------|----------------|-----------|
| `typeof()` | `Type` | Компиляция | Нет | 1.0 |
| `nameof()` | `string` | Компиляция | Нет | 6.0 |
| `sizeof()` | `int` | Компиляция* | Иногда** | 1.0 |

\* Для встроенных типов  
\** Для пользовательских структур

## Комплексный пример

```csharp
public class ReflectionDemo
{
    public string Name { get; set; }
    
    public void DemonstrateOperators()
    {
        // typeof - получаем метаданные типа
        Type type = typeof(ReflectionDemo);
        Console.WriteLine($"Тип: {type.Name}");
        Console.WriteLine($"Пространство имен: {type.Namespace}");
        
        // nameof - получаем имя свойства
        Console.WriteLine($"Имя свойства: {nameof(Name)}");
        
        // sizeof - получаем размер типа
        Console.WriteLine($"Размер int: {sizeof(int)} байт");
        
        // Комбинированное использование
        PropertyInfo prop = type.GetProperty(nameof(Name));
        Type propType = prop.PropertyType;
        
        if (propType == typeof(string))
        {
            Console.WriteLine($"Свойство {nameof(Name)} имеет тип string");
        }
    }
}
```

## Ключевые моменты для запоминания

### `typeof()`
- 🎯 Для получения метаданных типа
- 📦 Возвращает объект `Type`
- 🔍 Используется в рефлексии

### `nameof()`
- 🎯 Для получения имени элемента как строки
- 🛡️ Безопасность при рефакторинге
- 📝 Избегание магических строк

### `sizeof()`
- 🎯 Для определения размера типа
- ⚡ Работа с неуправляемой памятью
- 🔧 Низкоуровневое программирование

#C-Sharp #C-Sharp/Reflection #GENERATED
