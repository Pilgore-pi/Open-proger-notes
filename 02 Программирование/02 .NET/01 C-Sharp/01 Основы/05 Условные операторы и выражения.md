
Назад: [[02 Программирование/02 .NET/01 C-Sharp/01 Основы/04 Операции над переменными|Операции над переменными]]
Далее: [[02 Программирование/02 .NET/01 C-Sharp/01 Основы/06 Понятие выражений и операторов|Понятие выражений и операторов]]

> В C\# условные операторы `if-else` и `switch-case` являются **операторами**, а не выражениями, а тернарный условный оператор и оператор сопоставления с образцом `switch` являются **выражениями**, возвращающими какое-то значение

Переменную типа `Boolean` можно инициализировать такими выражениями:

```csharp
bool IsTrue = (5 > 6) || (4 < 6);
```
  
## Конструкция `if-else`

```csharp
// Оператор со скобками
if (a > b) {
    Console.WriteLine("a > b");
}
else {
    Console.WriteLine("b >= a");
}

// Оператор без скобок (допустимо только для одной строки кода)
if (a > b) Console.WriteLine("a > b");
else Console.WriteLine("b >= a");

// Аналогичный вариант с переносом строк
if (a > b)
    Console.WriteLine("a > b");
else
    Console.WriteLine("b >= a");
```

Переменная, объявленная внутри блока if-else существует только внутри области видимости этого блока. После выхода из области видимости переменная становится недоступной.

## Конструкция `switch-case`

```csharp
int choice = int.Parse(Console.ReadLine());

switch (choice) {
    case 1: Console.WriteLine(a + b); break;
    case 2: Console.WriteLine(a - b); break;
    case 3: Console.WriteLine(a * b); break;
    default: Console.WriteLine(0); break;
}

switch (choice) {
    case 1: return a + b;
    case 2: return a - b;
    case 3: return a * b;
    default: return 0;
}

switch (choice) {
    case 1: return a + b;
    case 2: goto Label;    // переход к метке
    case 3: return a * b;
    case 4:                // case 4 перенаправляется в блок default
        default: return 0;
}
```

Блоки `case` и `default` могут содержать блок кода неограниченного размера. Окончание блока `case` или блока `default` — это один из управляющих **операторов перехода**:

- `break` — явно говорит, что текущий блок `case` или `default` завершен
- `return` — немедленно завершает выполнение текущего метода и текущего оператора `switch-case`
- `case` — текущий блок переходит в другой блок `case`
- `default` — текущий блок `case` переходит в блок `default`
- `goto` — совершает переход к указанному блоку кода, помеченному меткой
- `throw` — генерирует исключение, системное прерывание программы, которое может быть обработано или привести к аварийному завершению программы

Более подробно об операторах перехода можно [[08 Операторы перехода|прочитать здесь]]

## Тернарная операция

> **Тернарная операция** (от слова "терра" - три (операнда)) — это **выражение**, которое возвращает одно из двух значений в зависимости от условия

```csharp
int x = 3, y = 2;

string operation = Console.ReadLine();

int z = operation == "+" ? (x + y) : (x - y);

//condition? iftrue : iffalse

// можно форматировать без особых ограничений
int z = operation == "+"
        ? x + y
        : x - y;
```

Далее: [[02 Программирование/02 .NET/01 C-Sharp/01 Основы/06 Понятие выражений и операторов|Понятие выражений и операторов]]

#C-Sharp #C-Sharp/If #C-Sharp/Cycles