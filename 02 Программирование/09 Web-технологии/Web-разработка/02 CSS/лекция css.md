Все элементы:
```css
*{
	color: red;
}
```

селектор — имя тега
```css
div{
	color: red;
}
```

идентификатор

```css
#test{
	color: red;
}
```

```html
<div id="test">...
```


`"."` — класс

```css
.test{
	color: red;
}
```

## Вложенность
`" "`
стиль для всех `<i>`, вложенных в `<p>`
```css
p i{
	color: red;
}
```

вложенность на 1 (непосредственная вложенность)
```css
p > i{
	color: red;
}
```
* Вместо селекторов тегов можно использовать любые другие типы селекторов

Перекрытие:
```css
div{
	color: red;
	margin: 5px;
}

div{
	color: blue;
	padding: 10px;
}
```

На приоритет применения стилей влияет:
* Порядок
* Сложность селектора (вложенность, параметры)
* Тип селектора

Существуют специальные формулы приоритетов для разных типов селекторов.

>Если стили не применяются по неизвестной причине, следует использовать валидатор для проверки ошибок синтаксиса.

>Браузеры применяют к странице маленький стиль по умолчанию, который может отличаться. Можно включить или отключить отображение встроенного стиля в режиме просмотра кода страницы.

## :: Псевдоклассы

~Неявно добавляют классы в html код

```css
a::before{
	content: "😎";
} стиль до тега
a::after{...} после тега
```

Классы
div с определенными классами: `menu` и `main`, вложенный в тег с определенным классом `wrapper`:
```css
.wrapper div.menu.main{...}
```

>Можно использовать препроцессор для генерации CSS\*

## Единицы измерения
Абсолютные единицы
```css
div{
font-size: 1px; /* может быть дробным */
font-size: 1in;
font-size: 1cm;
font-size: 1mm;
...
}
```

Относительные единицы
```
1% - процент родительского элемента
1em - множитель размера шрифта родительского элемента
1ex - высота "х" в нижнем регистре
1ch - Ширина "0"
1rem - размер шрифта коревого элемента (<html>, 16px)

1vw 1% от ширины окна просмотра (вкл. полосу прокрутки)
1vh - 1% от высоты окна просмотра (вкл. полосу прокрутки)
1vmin - наименьшее из vw & vh
1vmax - наибольшее из vw & vh
```

Вычисляемое выражение:
```css
div{
	font-size: calc(1em + 0.5vw)
	width: calc(100% - 50px)
}
```

>rgb, calc и др. — не функции

### Препроцессоры

Распространенные препроцессоры: Sass, LESS, SCSS

### Типы верстки

* Устаревшая верстка фреймами (нельзя использовать)
* Табличная верстка (устарела) —  не требует CSS
* Блочная верстка — более сложная, основан на блоках (дивах)
* "Float: left" — атрибут "обтекания" и использование margin
* display: grid (современный)
* display: flex (современный)


#Web #CSS