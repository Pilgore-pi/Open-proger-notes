
> Тип данных `System.String` — это неизменяемая упорядоченная последовательность символов в кодировке UTF-16

Если в программе определено несколько переменных типа `string` с одинаковым значением, то все эти переменные ссылаются на 1 объект, таким образом экономится память и повышается производительность за счет того, что не приходиться каждый раз в памяти выделять место под данные строки

При выполнении операций, меняющих строку (удаление символа, получение подстроки, добавление нового символа и т. д.) создается новый объект, а исходная строка не меняется и, если на эту строку не ссылается ни одна переменная, то, в последствии, исходная строка очищается сборщиком мусора

Для соблюдения описанных выше свойств, создание строки отличается от создания обычного объекта. В IL-коде используется специальная команда `ldstr "My string"`

Можно небезопасно создать объект строки, передав в конструктор `string()` переменную типа `*char` или `*sbyte`

## Литералы

Обычный литерал: `"text\r\n"`
Интерполяция: `$"number = {number}"`
Буквальная строка: `@"D:\Andrey\IT"`

Escape-последовательность `\U########` (будем называть расширенным символом) представляет собой символ или пару символов `char`

Если расширенный символ больше `\uffff`, то он представляется в виде двух символов Unicode с помощью суррогатной пары. Максимальное значение расширенного символа: `\U0010FFFF`

Можно именовать сущности кода с помощью расширенных символов:

```csharp
class \U00012345 { } // 🥺
"123450".To\U00000043harArray(); // \U00000043 — 'C'
```

> Расширенный символ нельзя применять для инициализации переменной `char`, так как он может занимать более 2 байт памяти

https://learn.microsoft.com/ru-ru/dotnet/standard/base-types/character-escapes-in-regular-expressions 

## Перевод на новую строку

Строковая последовательность обозначающая разрыв строки отличается от ОС к ОС:

- Windows: `\r\n`
- UNIX: `\n`
- Некоторые версии MacOS: `\r`

Чтобы использовать перенос строки независимо от ОС, следует использовать `Environment.NewLine`

## Внутренние механизмы работы со строками

* Конкатенация литеральных строк происходит на этапе компиляции; Конкатенация нелитеральных строк — на этапе выполнения
* При множественном редактировании исходной строки, следует использовать класс `StringBuilder`, который является **_изменяемым_** аналогом строки `string`, то есть при редактировании объекта `StringBuilder` новых объектов не создается
* `string` — запечатанный класс, это сделано для того, чтобы CLR однозначно могла знать, что перед ней неизменяемая строка с ожидаемыми полями и методами. Иначе можно было бы унаследоваться от строки и нарушить ее структуру и обмануть среду выполнения с помощью полиморфизма
* Сравнение строк можно выполнять с учетом региональных стандартов и с учетом регистра. Наиболее быстрый вариант сравнения StringComparison.Ordinal(IgnoreCase). Кроме того можно выполнять сравнение на совпадение символов, которое выражается в числе (`string.Compare`)

CLR создает хэш таблицу, где ключи — это строки, а значения — это ссылки на строковые объекты в управляемой куче. Можно получить доступ к внутренней хэш-таблице с помощью 2 методов:

Поиск строки в хэш-таблице и возвращение ссылки. Если строка не найдена, создается ее копия и помещается в хэш-таблицу, затем возвращается ссылка на эту строку.

```csharp
string.Intern(newText);
```

Поиск строки в хэш-таблице и возвращение ссылки. Если строка не найдена, возвращает `null`.

```csharp
string.IsIntern(someText);
```

Объекты `string`, на которые указывает хэш-таблица не могут быть удалены сборщиком мусора, пока не будет закрыт поток или выгружен соответствующий домен приложения

**Интернирование строк** — это помещение строк в хэш-таблицу. Этот процесс является затратным, поэтому обычно применяется специальный атрибут, который отключает автоматическое интернирование строк по умолчанию. Но это не гарантирует, что строка не будет интернирована

```csharp
String s1 = "Hello";
String s2 = "Hello";
// В некоторых версиях .NET 'False'
Console.WriteLine(Object.ReferenceEquals(s1, s2));

s1 = String.Intern(s1);
s2 = String.Intern(s2);
// 'True'
Console.WriteLine(Object.ReferenceEquals(s1, s2));
```

### Пул строк

При компиляции создается пул всех литеральных строк в метаданных программы. Все одинаковые строки ссылаются на 1 экземпляр. Такое поведение есть также в других языках, например C/C++.

### String.Empty

`String.Empty` — Это пустая строка длиной 0, которая не содержит символов. Она может быть конвертирована в пустой массив `char[]`
При этом литерал (`""`) может использоваться для определения констант, в то время, как `String.Empty` — readonly поле

```csharp
String.Empty == ""
```

Если можно, следует использовать поле `Empty` вместо литерала `""`, так как могут встречаться символы с нулевой шириной, которых, при этом может быть много. Например, внешне, строки `""` и `"﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿"` одинаковы, но на самом деле первая имеет длину 0, а вторая 20

* Использование `string.Empty` не позволяет компилятору выполнять оптимизацию конкатенации константных строк, что может привести к небольшому увеличению IL-кода

## Структура класса System.String

| Поле | Значение | Описание |
| :--- | :------- | :------- |
| `Empty` | `""` | Пустая строка нулевой ширины |

| Свойство | Описание |
| :- | :- |
| `this[int index]` | (Индексатор) Возвращает символ в указанной позиции только для чтения |
| `Length` | Возвращает длину строки |

| Метод экземпляра | Возвращает | Описание |
| :--------------- | :--------- | :------- |
| `Clone()` |  |  |
| `IndexOf()` | `int` |  |
| `IndexOfAny()` | `int` |  |
| `CompareTo()` | `int` |  |
| `Contains()` | `bool` |  |
| `CopyTo()` |  |  |
| `EndsWith()` | `bool` |  |
| `StartsWith()` | `bool` |  |
| `LastIndexOf()` | `int` |  |
| `LastIndexOfAny()` | `int` |  |
| `EnumerateRunes()` |  |  |
| `PadLeft()` | `string` |  |
| `PadRight()` | `string` |  |
| `Remove()` | `string` |  |
| `Replace()` | `string` |  |
| `ReplaceLineEndings()` | `string` |  |
| `Split()` | `string[]` |  |
| `ToCharArray()` | `char[]` |  |
| `ToLower()` | `string` |  |
| `ToUpper()` | `string` |  |
| `ToLowerInvariant()` | `string` |  |
| `ToUpperInvariant()` | `string` |  |
| `Trim()` | `string` |  |
| `TrimStart()` | `string` |  |
| `TrimEnd()` | `string` |  |
| `TryCopyTo()` | `bool` |  |

| Статический метод | Возвращает | Описание |
| :- | :- | :- |
| `Compare()` | `int` |  |
| `CompareOrdinal()` | `int` |  |
| `Concat()` | `string` |  |
| `Create()` | `string` |  |
| `Format()` | `string` |  |
| `Intern()` | `string` |  |
| `IsInterned()` |  | `bool` |
| `IsNullOrEmpty()` | `bool` |  |
| `IsNullOrWhiteSpace()` | `bool` |  |
| `Join()` | `string` |  |

[Форматирование дат] с 381 Рихтера

#C-Sharp #Char_types
