secpol.msc

there is 3 types of "Вход от имени":
network service (сетевая)
local service
local system

----

в конспект по библиотекам добавить аналогию: статическая библиотека — это орган человека, который нельзя поменять `сердце.lib`, `лёгкие.lib`, динамическая библиотека это внешняя одежда, которую легко можно поменять

#MERGE_NOTES